package com.bongtu.baekseo.core.common.type

enum class EventType(
    val label: String,
) {
    WEDDING(label = "결혼식"),
    FUNERAL(label = "장례식"),
    FIRST_BD(label = "돌잔치"),
    BIRTHDAY(label = "생일");
}
